$(document).ready(function () {
    // Initialization code
    $(document).foundation({
        abide: {
            live_validate: false
        }
    });

    // Scroll reveal pattern
    new scrollReveal({ mobile: true });
});